#include <iostream>
#include <windows.h>
using namespace std;


namespace classes
{
    static template<typename Toy>
    void functional_class(Toy load, VMCREATE::windows())
    {
        LOCALSYSTEMLOADERINIT psi;
        Toy local = psi.loader::load(sizeof(psi.init()));
        local.function::returntrue();
    {
}
