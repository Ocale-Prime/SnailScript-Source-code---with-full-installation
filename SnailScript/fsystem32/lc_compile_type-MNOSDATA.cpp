#include <windows.h>
using namespace std;

/* Define the local API */

namespace l_api
{
    virtual void function(int input_arg1, char input_arg2, double input_arg3);
    class c1_l1
    {
        // easer egg
        std::cout << "#BringBoomBack, we can do it, Skibidi fandom, W Boom[++]" << std::endl;

        TmCreateVmLoader tcvm;
        tcvm.loader = tcvm.loader32.function::load_engine();
    }
}
